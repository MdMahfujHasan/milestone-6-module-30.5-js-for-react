// variable: let, const
let money = 20;
money = 10;
const pName = 'Mahfuj';

let isHappy = true;

const person = {
    pName: 'Mahfuj',
    age: 23,
    city: 'Dhaka'
}

const statement = `this person ${pName} with age ${person.age} from ${person.city} city`;
console.log(statement);

// condition
if (money > 20 && age >= 45){

}
else{

}

// array
const number = [2, 3, 4];
const employee = ['Md.', 'Mahfuj', 'Hasan'];
const products = [{name: 'laptop', price: 500}, {name: 'mobile', price: 100}, {name: 'watch', price: 20}]

// loop: for loop
for (let i = 0; i < numbers.length; i++) {
    const number = numbers[i];

}

// function: regular
function add (num1, num2){
    return num1 + num2;
}
const result = add(5,6);

// function: arrow

const fiveTimes = num => num * 5;
const add2 = (num1, num2) => num1 + num2;
const doMath = (x, y) => {
    const sum = x + y;
    return sum * 2;
}